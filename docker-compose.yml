services:
  db:
    image: postgres:16-alpine
    container_name: plana-db
    environment:
      POSTGRES_USER: trello
      POSTGRES_PASSWORD: trello
      POSTGRES_DB: trello
    ports:
      - "5432:5432"
    volumes:
      - plana-db-data:/var/lib/postgresql/data

volumes:
  plana-db-data:
